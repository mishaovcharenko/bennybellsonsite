# Optional: override the canonical site URL used for Open Graph and metadata.
# Only set if your production URL differs from the default.
# NEXT_PUBLIC_* variables are exposed to the browser — never put secrets here.
# NEXT_PUBLIC_SITE_URL=https://bennybellson.netlify.app
